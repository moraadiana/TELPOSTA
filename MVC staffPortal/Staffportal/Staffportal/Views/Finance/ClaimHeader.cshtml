@model Staffportal.Models.Finance
@{
    ViewBag.Title = "Claim Header";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="row">
        <div class="col-lg-12 mb-4 order-0">
            <div class="card">
                <div class="card-body">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible" role="alert">
                            @TempData["Error"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success alert-dismissible" role="alert">
                            @TempData["Success"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="text-uppercase">Claim Requisition</h5>
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item active">Dashboard</li>
                                <li class="breadcrumb-item">
                                    <a href="@Url.Action("index","dashboard")">Claim Requisition</a>
                                </li>
                            </ol>
                        </nav>
                    </div>
                    <div class="dropdown-divider"></div>
                    <form action="@Url.Action("claimheader","finance")" method="post">
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label>Staff No</label><br />
                                <label class="text-primary">@Model.StaffNo</label>
                            </div>
                            <div class="col-md-6">
                                <label>Staff Name</label><br />
                                <label class="text-primary">@Model.StaffName</label>
                            </div>
                            @*<div class="col-md-3">
                                <label>Directorate</label><br />
                                <label class="text-primary">@Model.Directorate</label>
                            </div>
                            <div class="col-md-3">
                                <label>Department</label><br />
                                <label class="text-primary">@Model.Department</label>
                            </div>*@
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-4">
                                <label class="form-label">Responsibility Center</label>
                                @Html.DropDownListFor(m => m.ResponsibilityCenter, new SelectList(Model.ResponsibilityCenters, "Code", "Description"), "--Select Item--", new { @class = "form-control", required = "required" })
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Client Code</label>
                                @Html.DropDownListFor(m => m.ClientCode, new SelectList(Model.ClientCodes, "Code", "Description"), "--Select Item--", new { @class = "form-control", id = "clientCode" })
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Job Order Code</label>
                                @Html.DropDownListFor(m => m.JobOrderCode, new SelectList(Model.JobOrderCodes, "Code", "Description"), "--Select Item--", new { @class = "form-control", id = "jobOrderClients" })
                            </div>
                            @*<div class="col-md-3">
            <label class="form-label">Directorate</label>
            @Html.DropDownListFor(m => m.Directorate, new SelectList(Model.Directorates, "Code", "Description"), "--Select Item--", new { @class = "form-control", id = "directorate" })
        </div>
        <div class="col-md-3">
            <label class="form-label">Department</label>
            @Html.DropDownListFor(m => m.Department, new SelectList(Model.Departments, "Code", "Description"), "--Select Item--", new { @class = "form-control", id = "department" })
        </div>
        <div class="col-md-3">
            <label class="form-label">Project Code</label>
            @Html.DropDownListFor(m => m.Project, new SelectList(Model.Projects, "Code", "Description"), "--Select Item--", new { @class = "form-control", id = "project-code" })
        </div>
        <div class="col-md-3">
            <label class="form-label">Activity Code</label>
            <select name="Activity" class="form-control" id="activity-code"></select>
        </div>*@
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-12">
                                <label class="form-label">Purpose</label>
                                <textarea name="Purpose" class="form-control" placeholder="Type your purpose here..." required></textarea>
                            </div>
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-12">
                                <div class="d-flex justify-content-between align-items-center">
                                    <a href="@Url.Action("claimlisting","finance")" class="btn btn-warning">Back</a>
                                    <button type="submit" class="btn btn-primary">Next</button>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    //const projectCode = document.getElementById('project-code');
    //const activityCode = document.getElementById('activity-code');
    //const directorate = document.getElementById('directorate');
    //const department = document.getElementById('department');

    //directorate.value = '';
    //department.value = '';

    //const getActivities = async function () {
    //    const project = projectCode.value;
    //    activityCode.innerHTML = '';
    //    const response = await fetch(`/api/getactivities?project=${project}`);
    //    const result = await response.json();
    //    let html = "<option value=''>--Select Item--</option>";
    //    result.forEach(function (item, i, arr) {
    //        html += `<option value='${item.Code}'>${item.Code} => ${item.Description}</option>`;
    //    })
    //    activityCode.innerHTML = html;
    //}
    //getActivities();
    //projectCode.addEventListener('change', getActivities);
</script>